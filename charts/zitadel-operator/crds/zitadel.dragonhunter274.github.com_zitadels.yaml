---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: zitadels.zitadel.dragonhunter274.github.com
spec:
  group: zitadel.dragonhunter274.github.com
  names:
    kind: Zitadel
    listKind: ZitadelList
    plural: zitadels
    singular: zitadel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Zitadel is the Schema for the zitadels API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZitadelSpec defines the desired state of a Zitadel instance.
            properties:
              configuration:
                description: |-
                  Configuration holds freeform Zitadel YAML config (non-sensitive).
                  Rendered into a ConfigMap and mounted at /config/zitadel-config.yaml.
                x-kubernetes-preserve-unknown-fields: true
              database:
                description: Database configures the PostgreSQL connection.
                properties:
                  adminCredentials:
                    description: |-
                      AdminCredentials references a Secret with keys "username" and "password"
                      for the admin database user (used by init and setup jobs).
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                  database:
                    default: zitadel
                    description: Database is the database name.
                    type: string
                  host:
                    description: Host is the PostgreSQL server hostname.
                    type: string
                  port:
                    default: 5432
                    description: Port is the PostgreSQL server port.
                    format: int32
                    type: integer
                  sslMode:
                    default: disable
                    description: SSLMode for PostgreSQL connections.
                    enum:
                    - disable
                    - require
                    - verify-ca
                    - verify-full
                    type: string
                  sslRootCert:
                    description: SSLRootCert references a Secret containing the CA
                      certificate.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                  userCredentials:
                    description: |-
                      UserCredentials references a Secret with keys "username" and "password"
                      for the application database user (used at runtime).
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - adminCredentials
                - host
                - userCredentials
                type: object
              firstInstance:
                description: FirstInstance configures the initial instance bootstrapped
                  by "zitadel setup".
                properties:
                  org:
                    description: Org configures the default organization.
                    properties:
                      human:
                        description: Human configures the initial admin user.
                        properties:
                          email:
                            description: Email address.
                            type: string
                          firstName:
                            description: FirstName of the admin.
                            type: string
                          lastName:
                            description: LastName of the admin.
                            type: string
                          passwordRef:
                            description: PasswordRef references a Secret containing
                              key "password".
                            properties:
                              key:
                                description: Key within the Secret.
                                type: string
                              name:
                                description: Name of the Secret.
                                type: string
                            required:
                            - name
                            type: object
                          userName:
                            description: UserName for the admin.
                            type: string
                        required:
                        - email
                        - firstName
                        - lastName
                        - passwordRef
                        - userName
                        type: object
                      name:
                        description: Name of the default organization.
                        type: string
                    required:
                    - human
                    - name
                    type: object
                required:
                - org
                type: object
              ingress:
                description: Ingress configures optional Ingress resource creation.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations for the Ingress resource.
                    type: object
                  enabled:
                    description: Enabled toggles Ingress creation.
                    type: boolean
                  ingressClassName:
                    description: IngressClassName specifies the Ingress controller
                      class.
                    type: string
                  tls:
                    description: TLS configuration for the Ingress.
                    items:
                      description: IngressTLS describes the transport layer security
                        associated with an ingress.
                      properties:
                        hosts:
                          description: |-
                            hosts is a list of hosts included in the TLS certificate. The values in
                            this list must match the name/s used in the tlsSecret. Defaults to the
                            wildcard host setting for the loadbalancer controller fulfilling this
                            Ingress, if left unspecified.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        secretName:
                          description: |-
                            secretName is the name of the secret used to terminate TLS traffic on
                            port 443. Field is left optional to allow TLS routing based on SNI
                            hostname alone. If the SNI host in a listener conflicts with the "Host"
                            header field used by an IngressRule, the SNI host is used for termination
                            and value of the "Host" header is used for routing.
                          type: string
                      type: object
                    type: array
                required:
                - enabled
                type: object
              masterkey:
                description: |-
                  Masterkey references the Secret containing the 32-char encryption key.
                  The Secret must have a key named "masterkey".
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              network:
                description: Network configures external access and TLS.
                properties:
                  externalDomain:
                    description: ExternalDomain is the user-facing domain (e.g. "auth.example.com").
                    type: string
                  externalPort:
                    default: 443
                    description: ExternalPort is the user-facing port.
                    format: int32
                    type: integer
                  externalSecure:
                    default: true
                    description: ExternalSecure indicates whether TLS is terminated
                      externally.
                    type: boolean
                  tls:
                    description: TLS configures TLS termination on the Zitadel pod
                      itself.
                    properties:
                      enabled:
                        description: Enabled toggles TLS on the Zitadel pod.
                        type: boolean
                      secretName:
                        description: SecretName references a TLS Secret (tls.crt,
                          tls.key).
                        type: string
                    required:
                    - enabled
                    type: object
                required:
                - externalDomain
                type: object
              replicas:
                default: 1
                description: Replicas for the main Zitadel Deployment.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines compute resources for the Zitadel containers.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              secretConfiguration:
                description: |-
                  SecretConfiguration references a Secret containing sensitive YAML
                  config overrides mounted after the ConfigMap.
                properties:
                  key:
                    description: Key within the Secret.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                required:
                - name
                type: object
              serviceAccount:
                description: |-
                  ServiceAccount configures the operator-managed service account
                  used for API-resource controllers to authenticate to Zitadel.
                properties:
                  secretName:
                    description: SecretName where the operator writes the PAT after
                      creating it.
                    type: string
                  userName:
                    default: zitadel-operator
                    description: UserName for the machine user the operator uses to
                      call Zitadel APIs.
                    type: string
                type: object
              version:
                description: Version is the Zitadel container image tag (e.g. "v2.67.2").
                minLength: 1
                type: string
            required:
            - database
            - masterkey
            - network
            - version
            type: object
          status:
            description: ZitadelStatus defines the observed state of a Zitadel instance.
            properties:
              conditions:
                description: Conditions follow the standard Kubernetes conditions
                  pattern.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last .metadata.generation reconciled.
                format: int64
                type: integer
              phase:
                description: 'Phase is a high-level summary: Pending, Initializing,
                  Running, Error.'
                enum:
                - Pending
                - Initializing
                - Running
                - Error
                type: string
              ready:
                description: Ready is the number of ready replicas.
                format: int32
                type: integer
              serviceAccountReady:
                description: |-
                  ServiceAccountReady indicates whether the operator service account
                  has been created and its PAT is available.
                type: boolean
              version:
                description: Version is the currently deployed Zitadel version.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
