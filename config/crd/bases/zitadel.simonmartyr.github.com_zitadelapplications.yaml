---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: zitadelapplications.zitadel.dragonhunter274.github.com
spec:
  group: zitadel.dragonhunter274.github.com
  names:
    kind: ZitadelApplication
    listKind: ZitadelApplicationList
    plural: zitadelapplications
    singular: zitadelapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.clientID
      name: ClientID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZitadelApplication is the Schema for the zitadelapplications
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZitadelApplicationSpec defines the desired state of a Zitadel
              application.
            properties:
              api:
                description: API settings (required when type is API).
                properties:
                  authMethodType:
                    default: BASIC
                    description: 'AuthMethodType: BASIC, PRIVATE_KEY_JWT.'
                    enum:
                    - BASIC
                    - PRIVATE_KEY_JWT
                    type: string
                type: object
              clientSecretRef:
                description: |-
                  ClientSecretRef optionally names a Secret where generated client
                  credentials (client_id, client_secret) are written.
                type: string
              name:
                description: Name of the application.
                type: string
              oidc:
                description: OIDC settings (required when type is OIDC).
                properties:
                  accessTokenType:
                    default: BEARER
                    description: 'AccessTokenType: BEARER, JWT.'
                    type: string
                  appType:
                    default: WEB
                    description: 'AppType: WEB, USER_AGENT, NATIVE.'
                    enum:
                    - WEB
                    - USER_AGENT
                    - NATIVE
                    type: string
                  authMethodType:
                    default: BASIC
                    description: 'AuthMethodType: BASIC, POST, NONE, PRIVATE_KEY_JWT.'
                    enum:
                    - BASIC
                    - POST
                    - NONE
                    - PRIVATE_KEY_JWT
                    type: string
                  devMode:
                    default: false
                    description: DevMode enables development mode (allows http redirect
                      URIs).
                    type: boolean
                  grantTypes:
                    default:
                    - AUTHORIZATION_CODE
                    description: 'GrantTypes: AUTHORIZATION_CODE, IMPLICIT, REFRESH_TOKEN,
                      DEVICE_CODE.'
                    items:
                      type: string
                    type: array
                  postLogoutRedirectUris:
                    description: PostLogoutRedirectURIs for post-logout.
                    items:
                      type: string
                    type: array
                  redirectUris:
                    description: RedirectURIs for OIDC redirect.
                    items:
                      type: string
                    type: array
                  responseTypes:
                    default:
                    - CODE
                    description: 'ResponseTypes: CODE, ID_TOKEN, ID_TOKEN_TOKEN.'
                    items:
                      type: string
                    type: array
                required:
                - redirectUris
                type: object
              projectRef:
                description: ProjectRef names the ZitadelProject in the same namespace.
                type: string
              saml:
                description: SAML settings (required when type is SAML).
                properties:
                  metadataUrl:
                    description: MetadataURL is the URL to fetch SAML metadata.
                    type: string
                  metadataXml:
                    description: MetadataXML is the raw SAML metadata.
                    type: string
                type: object
              type:
                description: Type is the application type.
                enum:
                - OIDC
                - SAML
                - API
                type: string
              zitadelRef:
                description: ZitadelRef names the Zitadel instance in the same namespace.
                type: string
            required:
            - name
            - projectRef
            - type
            - zitadelRef
            type: object
          status:
            description: ZitadelApplicationStatus defines the observed state of a
              Zitadel application.
            properties:
              appID:
                description: AppID is the Zitadel-assigned application ID.
                type: string
              clientID:
                description: ClientID is the OIDC/API client ID.
                type: string
              conditions:
                description: Conditions follow the standard Kubernetes conditions
                  pattern.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last .metadata.generation reconciled.
                format: int64
                type: integer
              ready:
                description: Ready indicates the application exists and is active.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
