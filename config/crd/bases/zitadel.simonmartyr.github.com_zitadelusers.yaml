---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: zitadelusers.zitadel.dragonhunter274.github.com
spec:
  group: zitadel.dragonhunter274.github.com
  names:
    kind: ZitadelUser
    listKind: ZitadelUserList
    plural: zitadelusers
    singular: zitadeluser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.userID
      name: UserID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZitadelUser is the Schema for the zitadelusers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZitadelUserSpec defines the desired state of a Zitadel user.
            properties:
              credentialSecretRef:
                description: |-
                  CredentialSecretRef optionally names a Secret where credentials
                  (PAT or generated password) are written back.
                type: string
              human:
                description: Human profile (required when type is Human).
                properties:
                  email:
                    description: Email address.
                    type: string
                  firstName:
                    description: FirstName of the user.
                    type: string
                  lastName:
                    description: LastName of the user.
                    type: string
                  passwordRef:
                    description: PasswordRef references a Secret containing the initial
                      password.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - email
                - firstName
                - lastName
                type: object
              machine:
                description: Machine profile (required when type is Machine).
                properties:
                  description:
                    description: Description of the machine user.
                    type: string
                  generatePAT:
                    default: false
                    description: GeneratePAT tells the controller to create a Personal
                      Access Token.
                    type: boolean
                  name:
                    description: Name is the display name.
                    type: string
                required:
                - name
                type: object
              organizationRef:
                description: OrganizationRef names the ZitadelOrganization in the
                  same namespace.
                type: string
              type:
                description: 'Type: Human or Machine.'
                enum:
                - Human
                - Machine
                type: string
              userName:
                description: UserName is the login name.
                type: string
              zitadelRef:
                description: ZitadelRef names the Zitadel instance in the same namespace.
                type: string
            required:
            - type
            - userName
            - zitadelRef
            type: object
          status:
            description: ZitadelUserStatus defines the observed state of a Zitadel
              user.
            properties:
              conditions:
                description: Conditions follow the standard Kubernetes conditions
                  pattern.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the last .metadata.generation reconciled.
                format: int64
                type: integer
              ready:
                description: Ready indicates the user exists and is active.
                type: boolean
              userID:
                description: UserID is the Zitadel-assigned user ID.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
